# 32. `call`, `apply` & `bind`

### Must Implement

1. Implement `call()`.
2. Implement `apply()`.
3. Implement `bind()`.

Also understand:

- How `this` is passed.
- Why `bind()` returns a new function.
- Difference between the three methods.
